# Global Instructions

- If it's a UI, use only vanilla HTML/CSS/JavaScript unless the user specifies something different.
- When possible, use Rust over other languages. Always run `cargo fmt` and `cargo clippy` after changes.
- Never commit or push files to git, the user will take care of that.
