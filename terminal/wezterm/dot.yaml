global:
  links:
    wezterm.lua: ~/.config/wezterm/wezterm.lua

linux:
  installs: sudo dnf -y copr enable wezfurlong/wezterm-nightly && sudo dnf -y install wezterm wl-clipboard nu && chsh -s /usr/bin/nu
