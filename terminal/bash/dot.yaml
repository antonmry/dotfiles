global:
  links:
    bashrc: ~/.bashrc
    bash_profile: ~/.bash_profile
    profile: ~/.profile
    bash_logout: ~/.bash_logout
  installs: false

linux:
  installs: sudo dnf -y copr enable atim/zoxide && sudo dnf -y install zoxide && sudo dnf -y install fzf bat
